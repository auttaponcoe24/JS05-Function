// โค้ดทั้งสองชุดด้านล่างทำงานได้เหมือนกันหรือไม่

// ```js
const logName1 = (name) => console.log(name); 
const logName2 = name => console.log(name); 
// ```
 // answer ทำงานได้เหมือนกัน สร้าง arrow fn แบบสั้น หรือมี parametor ตัวเดียว ไม่จำเป็นต้องมี () ก็ได้