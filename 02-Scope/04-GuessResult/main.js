// ผลลัพธ์ในบรรทัดที่มี * มีค่าเป็นอะไรและเพราะอะไร

// ```js
let x = 1;
function func() {
  console.log(x); // * error เพราะให้ fn กำหนดตัวแปร หลัง log มีการเรียกใช้ ตัวแปร ก่อนที่จะประกาศ let x=2 
  let x = 2;
}
func();
// ```